import {PureComponent} from "react"
import ssjs from "ssjs"

import theme from "./helpers/theme.tea"

class Checkbox extends PureComponent {
    render() => {
        return <doric-checkbox>
            Checkbox!
        </doric-checkbox>
    }
}

export default Checkbox
